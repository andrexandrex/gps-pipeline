name: GPS Pipeline CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  PYTHON_VERSION: "3.12"
  AWS_DEFAULT_REGION: us-east-1

jobs:
  # ── 1. Unit tests (no AWS needed) ──────────────────────────────────────────
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements-dev.txt

      - name: Run unit tests
        run: |
          PYTHONPATH=src pytest \
            tests/test_validate_gps.py \
            tests/test_batch.py \
            tests/test_quality.py \
            -k "not Integration" \
            -v --tb=short

  # ── 2. Integration tests (LocalStack in CI) ─────────────────────────────────
  integration-tests:
    name: Integration Tests (LocalStack)
    runs-on: ubuntu-latest
    needs: unit-tests
    services:
      localstack:
        image: localstack/localstack:3.4
        ports:
          - 4566:4566
        env:
          SERVICES: s3,kinesis,dynamodb,sns,sqs,lambda,glue,iam
          DEBUG: 0
        options: >-
          --health-cmd "curl -f http://localhost:4566/_localstack/health"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
          --health-start-period 30s

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements-dev.txt

      - name: Bootstrap LocalStack resources
        env:
          AWS_ACCESS_KEY_ID: test
          AWS_SECRET_ACCESS_KEY: test
          AWS_ENDPOINT_URL: http://localhost:4566
        run: bash infra/scripts/bootstrap.sh

      - name: Run integration tests
        env:
          AWS_ACCESS_KEY_ID: test
          AWS_SECRET_ACCESS_KEY: test
          AWS_DEFAULT_REGION: us-east-1
          AWS_ENDPOINT_URL: http://localhost:4566
        run: |
          PYTHONPATH=src pytest tests/test_integration.py tests/test_batch.py \
            -v --tb=short -k "Integration"

  # ── 3. Terraform plan (on PRs, no apply) ────────────────────────────────────
  terraform-plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    needs: unit-tests
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.8.0"

      - name: Terraform Init (no backend for plan)
        working-directory: infra/terraform
        run: terraform init -backend=false

      - name: Terraform Validate
        working-directory: infra/terraform
        run: terraform validate
        env:
          AWS_ACCESS_KEY_ID: test
          AWS_SECRET_ACCESS_KEY: test
          AWS_DEFAULT_REGION: us-east-1

      - name: Terraform Plan (syntax check only, LocalStack vars)
        working-directory: infra/terraform
        run: terraform plan -var="use_localstack=true" -var="aws_account_id=000000000000" -out=tfplan
        env:
          AWS_ACCESS_KEY_ID: test
          AWS_SECRET_ACCESS_KEY: test
          AWS_DEFAULT_REGION: us-east-1

  # ── 4. Package Lambda zips ──────────────────────────────────────────────────
  package-lambdas:
    name: Package Lambdas
    runs-on: ubuntu-latest
    needs: unit-tests
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Build Lambda packages
        run: |
          mkdir -p build
          for LAMBDA in validate_gps detect_signal_loss; do
            echo "Packaging $LAMBDA..."
            BUILD_DIR="/tmp/lambda-$LAMBDA"
            mkdir -p "$BUILD_DIR"
            pip install -q --target "$BUILD_DIR" boto3 pandas pyarrow
            cp -r "src/lambdas/$LAMBDA/." "$BUILD_DIR/"
            cp -r "src/lambdas/common" "$BUILD_DIR/common"
            (cd "$BUILD_DIR" && zip -qr "$GITHUB_WORKSPACE/build/${LAMBDA}.zip" .)
            echo "  $(du -sh "build/${LAMBDA}.zip" | cut -f1) → build/${LAMBDA}.zip"
          done
          # Package batch job
          BUILD_DIR="/tmp/lambda-ingest"
          mkdir -p "$BUILD_DIR"
          pip install -q --target "$BUILD_DIR" boto3 pandas pyarrow
          cp -r "src/batch/." "$BUILD_DIR/"
          cp -r "src/lambdas/common" "$BUILD_DIR/common"
          (cd "$BUILD_DIR" && zip -qr "$GITHUB_WORKSPACE/build/ingest_maintenance.zip" .)

      - name: Upload Lambda artifacts
        uses: actions/upload-artifact@v4
        with:
          name: lambda-zips
          path: build/*.zip
          retention-days: 7

  # ── 5. Deploy to AWS (main branch only, requires approval) ─────────────────
  deploy:
    name: Deploy to AWS
    runs-on: ubuntu-latest
    needs: [integration-tests, package-lambdas]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment: production   # requires manual approval in GitHub Environments
    steps:
      - uses: actions/checkout@v4

      - name: Download Lambda artifacts
        uses: actions/download-artifact@v4
        with:
          name: lambda-zips
          path: build/

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.8.0"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ env.AWS_DEFAULT_REGION }}

      - name: Get AWS account ID and ensure state bucket exists
        id: aws_info
        run: |
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          echo "account_id=$ACCOUNT_ID" >> $GITHUB_OUTPUT
          STATE_BUCKET="gps-tfstate-${ACCOUNT_ID}"
          echo "state_bucket=$STATE_BUCKET" >> $GITHUB_OUTPUT
          aws s3api head-bucket --bucket "$STATE_BUCKET" 2>/dev/null || \
            aws s3api create-bucket --bucket "$STATE_BUCKET" --region ${{ env.AWS_DEFAULT_REGION }}
          aws s3api put-bucket-versioning --bucket "$STATE_BUCKET" \
            --versioning-configuration Status=Enabled

      - name: Terraform Apply (real AWS)
        working-directory: infra/terraform
        env:
          AWS_DEFAULT_REGION: ${{ env.AWS_DEFAULT_REGION }}
        run: |
          terraform init \
            -backend-config="bucket=${{ steps.aws_info.outputs.state_bucket }}" \
            -backend-config="key=gps-pipeline/terraform.tfstate" \
            -backend-config="region=${{ env.AWS_DEFAULT_REGION }}"
          terraform apply -auto-approve \
            -var="use_localstack=false" \
            -var="aws_account_id=${{ steps.aws_info.outputs.account_id }}"

      - name: Deploy Lambda functions
        run: bash infra/scripts/deploy_lambdas.sh

      - name: Smoke test — verify resources exist
        run: |
          echo "=== S3 buckets ==="
          aws s3 ls | grep gps-
          echo "=== SQS queues ==="
          aws sqs list-queues --query QueueUrls
          echo "=== DynamoDB tables ==="
          aws dynamodb list-tables --query TableNames
          echo "=== Lambda functions ==="
          aws lambda list-functions --query 'Functions[?contains(FunctionName, `validate`) || contains(FunctionName, `detect`) || contains(FunctionName, `ingest`)].FunctionName'
